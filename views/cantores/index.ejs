<%- include("../components/head.ejs") %>
<%- include("../components/menu.ejs") %>

<div class="container">
  <div class="action-area">
    <h1>Lista de Cantores</h1>
    <button id='btn-add' type='button' class='btn btn-outline-success' onclick="window.location.href='/cantores/add-cantores'">Nova +</button>
  </div>
  <div class="table-area">
    <table class='table table-bordered'>
      <thead>
        <th>Nome</th>
        <th></th>
      </thead>
      <tbody>
        <% cantores.forEach(cantor => { %>
          <tr>
            <td><%= cantor.nome %></td>
            <td id="button-area">
              <button onclick="window.location.href='/cantores/excluir/<%= cantor.id %>'" id="deleteButton">
                <i class="fas fa-trash" id="icon-trash"></i>
              </button>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>

<%- include("../components/footer.ejs") %>